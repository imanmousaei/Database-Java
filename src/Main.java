import java.util.Scanner;

import static io.io.*;

public class Main {
    public static void main(String[] args) {
        String input = readJSONObject(System.in);
        processInput(input);
    }
}
